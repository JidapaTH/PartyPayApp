<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- App bar style -->
    <style name="TransactionAppBarStyle" parent="style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <!-- Remove the shadow below the app bar -->
        <item name="elevation">0dp</item>
    </style>

    <!-- Style for a tab that displays a category name -->
    <style name="TransactionTab" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/textOnP</item>
        <item name="tabSelectedTextColor">@color/textOnP</item>
        <item name="tabBackground">@color/colorPrimary</item>
        <item name="tabTextAppearance">@style/TransactionTabTextAppearance</item>
    </style>

    <!-- Text appearance style for a category tab -->
    <style name="TransactionTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@color/textOnP</item>
    </style>
</resources>
